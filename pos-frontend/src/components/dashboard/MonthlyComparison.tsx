import { ArrowDownRight, ArrowUpRight } from 'lucide-react';

export default function MonthlyComparison({ current, previous }: { current: number; previous: number }) { const change = previous ? ((current - previous) / previous) * 100 : current ? 100 : 0; const positive = change >= 0; return <section className="rounded-xl bg-white p-5 shadow-sm ring-1 ring-gray-100"><p className="text-sm font-medium text-gray-500">Comparaison mensuelle</p><p className="mt-2 text-2xl font-bold text-gray-900">{current.toLocaleString('fr-FR')} F CFA</p><div className={`mt-3 inline-flex items-center gap-1 rounded-full px-2 py-1 text-xs font-semibold ${positive ? 'bg-green-50 text-green-700' : 'bg-red-50 text-red-700'}`}>{positive ? <ArrowUpRight className="h-3 w-3" /> : <ArrowDownRight className="h-3 w-3" />}{Math.abs(change).toFixed(1)}% <span className="font-normal">vs mois dernier</span></div></section>; }
